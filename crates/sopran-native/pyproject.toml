[build-system]
requires = ["maturin>=1.9,<2"]
build-backend = "maturin"

[project]
name = "sopran-native"
version = "0.1.0"
description = "Native extension helpers for SOPRAN"
requires-python = ">=3.11"

[tool.maturin]
module-name = "sopran_native"
features = ["extension-module"]
